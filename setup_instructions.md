# Setup Instructions

1. Clone the repository from GitHub.
2. Open the project folder.
3. Install the required libraries using `pip install -r requirements.txt`.
4. Go to the `src` folder.
5. Run the Python file to start the predictive maintenance model.
6. The system will train the model and display prediction results and performance metrics.
